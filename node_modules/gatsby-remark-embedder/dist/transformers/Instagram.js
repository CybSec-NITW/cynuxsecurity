"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getHTML = exports.shouldTransform = void 0;

var _utils = require("./utils");

const shouldTransform = url => {
  const {
    host,
    pathname
  } = new URL(url);
  return ['instagr.am', 'www.instagr.am', 'instagram.com', 'www.instagram.com'].includes(host) && pathname.includes('/p/');
};

exports.shouldTransform = shouldTransform;

const getHTML = url => (0, _utils.fetchOEmbedData)(`https://api.instagram.com/oembed?url=${url}&omitscript=true`).then(({
  html
}) => html);

exports.getHTML = getHTML;